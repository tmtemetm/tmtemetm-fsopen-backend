{
  "name": "tmtemetm-fsopen-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "rm -rf dist && cd ../tmtemetm-fsopen-frontend/ && npm run build && cp -r dist ../tmtemetm-fsopen-backend/",
    "deploy": "git add . && git commit -m build && git push",
    "deploy:full": "npm run build:ui && npm run deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tmtemetm/tmtemetm-fsopen-backend.git"
  },
  "author": "tmtemetm",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/tmtemetm/tmtemetm-fsopen-backend/issues"
  },
  "homepage": "https://github.com/tmtemetm/tmtemetm-fsopen-backend#readme",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.4.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
